export interface Variant {
    back: string;
    border: string;
    color: string;
};

const variants : Variant[]=
[
    { back : 'back-cup-wider', border : 'border-round', color : 'gold' },
    { back : 'back-cup-wider', border : 'border-round', color : 'charcoal' },
]

export default variants;